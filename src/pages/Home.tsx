function Home() {
  return (
    <div className="container mx-auto flex h-[100vh] items-center justify-center bg-red-50">
      <h1>
        This Project is bootstrapped with React + Vite + ESLint + Prettier +
        TailwindCSS + reduxtoolkit + Typescript + Airbnb style guide
      </h1>
    </div>
  );
}

export default Home;
